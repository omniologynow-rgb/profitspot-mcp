version: 1
runtime: python
entry: src/profitspot_mcp/server.py

build:
  install: pip install --no-cache-dir .

startCommand:
  type: stdio
  command: python -m profitspot_mcp.server

configSchema:
  source: code
